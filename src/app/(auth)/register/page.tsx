import { OfflineDemoNotice } from "@/components/demo/OfflineDemoNotice";

export default function RegisterPage() {
  return (
    <OfflineDemoNotice
      title="Create an account"
      description="Self-registration is managed by your estate office. Return home or sign in if you were invited as a resident, guard, or administrator."
    />
  );
}
