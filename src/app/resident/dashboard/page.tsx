import { OfflineDemoNotice } from "@/components/demo/OfflineDemoNotice";

export default function ResidentDashboardPage() {
  return (
    <OfflineDemoNotice
      title="Resident dashboard"
      description="Your resident dashboard lives under My home. Sign in from the home page to pre-register guests and view expected arrivals."
    />
  );
}
