import { OfflineDemoNotice } from "@/components/demo/OfflineDemoNotice";

export default function ResidentLoginPage() {
  return (
    <OfflineDemoNotice
      title="Resident login"
      description="Residents sign in from the home page. Choose My home to open your unit dashboard and manage visitors."
    />
  );
}
